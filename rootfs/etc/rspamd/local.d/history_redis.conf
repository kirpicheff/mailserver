servers = 127.0.0.1:6379; # Redis server to store history
key_prefix = "rs_history"; # Default key name
nrows = 1000; # Default rows limit
compress = true; # Use zstd compression when storing data in redis
